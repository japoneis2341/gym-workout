<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32">
  <!-- Fundo preto -->
  <rect width="32" height="32" rx="6" fill="#0a0a0a"/>
  
  <!-- Barra de haltere -->
  <!-- Cabo central -->
  <rect x="7" y="14.5" width="18" height="3" rx="1.5" fill="#e8ff00"/>
  <!-- Peso esquerdo externo -->
  <rect x="4" y="11" width="4" height="10" rx="1.5" fill="#e8ff00"/>
  <!-- Peso esquerdo interno -->
  <rect x="7.5" y="12.5" width="2.5" height="7" rx="1" fill="#e8ff00"/>
  <!-- Peso direito interno -->
  <rect x="22" y="12.5" width="2.5" height="7" rx="1" fill="#e8ff00"/>
  <!-- Peso direito externo -->
  <rect x="24" y="11" width="4" height="10" rx="1.5" fill="#e8ff00"/>

  <!-- Letra G estilizada no meio (opcional — removida para ficar mais clean) -->
</svg>
